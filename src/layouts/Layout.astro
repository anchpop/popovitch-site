<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Andre Popovitch</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --text-color: #333;
    --bg-color: #f5f5f5;
    --card-bg: #ffffff;
    --accent-color: #3245ff;
    --secondary-color: #bc52ee;
    --accent-color-rgb: 50, 69, 255;
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --text-color: #e0e0e0;
      --bg-color: #121212;
      --card-bg: #1e1e1e;
      --accent-color: #5c6aff;
      --secondary-color: #d67aff;
      --accent-color-rgb: 92, 106, 255;
    }
  }

  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
    font-family: Inter, Roboto, "Helvetica Neue", "Arial Nova", "Nimbus Sans",
      Arial, sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    transition:
      background-color 0.3s ease,
      color 0.3s ease;
  }

  a {
    color: var(--accent-color);
  }
</style>
